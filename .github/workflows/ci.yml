name: prachi cloudKart CI Pipeline

on:
  push:
    branches:
      - main
jobs:
  frontend-build:
    runs-on: ubuntu-latest
    

    steps:
      - name: code-checkout
        uses: actions/checkout@v6

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install dependencies
        working-directory: ./Frontend
        run: npm install

      - name: Build React App
        working-directory: ./Frontend
        run: npm run build

  backend-build:
    runs-on: ubuntu-latest
    needs: frontend-build

    steps:
      - name: code-checkout
        uses: actions/checkout@v6

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install dependencies
        working-directory: ./Backend
        run: npm install

      - name: Backend Validation
        run: echo "Backend Build Successful"

  
  docker-build:
    runs-on: ubuntu-latest
    needs: [frontend-build,backend-build,database-build]

    steps:
      - name: code-checkout
        uses: actions/checkout@v6

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build-Database-Image
        working-directory: ./Database
        run: docker image build -t prachi-postgres:1.0 .

      - name: Tagging and Pusing into DockerHub
        run : |
              docker image tag prachi-postgres:1.0 ${{ vars.DOCKER_USERNAME }}/prachi-postgres:latest
              docker push ${{ vars.DOCKER_USERNAME }}/prachi-postgres:latest

      - name: Buil-frontend-Image
        working-directory: ./Frontend
        run: docker image build -t frontend:1.0 .

      - name: Tagging and Pusing into DockerHub
        run : |
              docker image tag frontend:1.0 ${{ vars.DOCKER_USERNAME }}/frontend:latest
              docker push ${{ vars.DOCKER_USERNAME }}/frontend:latest

      - name: Build-Backend-Image
        working-directory: ./Backend
        run: docker image build -t backend:1.0 .

      - name: Tagging and Pusing into DockerHub
        run : |
              docker image tag backend:1.0 ${{ vars.DOCKER_USERNAME }}/backend:latest
              docker push ${{ vars.DOCKER_USERNAME }}/backend:latest
      


      
